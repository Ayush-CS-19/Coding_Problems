#include<bits/stdc++.h>
using namespace std;
#define LL long long

vector<int>ans;
void Solve(vector<int>& rains){
    int n = rains.size();
    
}
int main(){
    int n;
    cin>>n;
    vector<int>arr(n,0);
    for(int i = 0; i < n; i++)cin>>arr[i];
    Solve(arr);
    return 0;
}